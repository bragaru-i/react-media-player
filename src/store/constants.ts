import { MediaProperty } from '.';

export const DEFAULT_MEDIA_PROPERTY: MediaProperty = {
  currentTime: 0,
  playbackRate: 1,
  duration: 0,
  volume: 1,
  ready: false,
  hasPlayedOrSeeked: false,
  paused: false,
  muted: false,
};
